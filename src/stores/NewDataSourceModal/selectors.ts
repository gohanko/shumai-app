import { NewDataSourceModalnState } from "./store";

export const isModalOpenSelector = (state: any) =>  state.isModalOpen
export const toggleIsModalOpenSelector = (state: any) => state.toggleIsModalOpen