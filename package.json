{
  "name": "shumai-rpc",
  "version": "1.0.0",
  "description": "RPC server for [shumai-app](https://github.com/gohanko/shumai-app) responsible for running plugins (DataLoader, DataTransformer).",
  "main": "index.js",
  "scripts": {
    "start": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-rpc-2.0": "^1.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
